/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // L'app vit sous /scraper dans la suite Charlie (portail sur le port 3005)
  basePath: '/scraper',
};

module.exports = nextConfig;
